{% extends "Backend/Templates/template.twig" %}

{% block head_title %}
    Ajouter un article
{% endblock %}

{% block content %}
    <div id='addArticleForm' class='bg-light mt-5 pt-5 pb-3'>
        <h1 class="text-center">Ajouter un article</h1>
        <form action='/admin/addArticle' method="POST" class="offset-lg-2 col-lg-8 offset-1 col-10 my-5 pb-5">
            <div class="form-group">
                <label for="title">Titre du chapitre*</label>
                <input type="text" class="form-control" id="title" name="title" required>
            </div>
            <div class="form-group">
                <label for="imageURL">URL de l'image d'illustration*</label>
                <input type="url" class="form-control" id="imageURL" name="imageURL" required>
            </div>
            <div class="form-group">
                <label for="imageDescription">Description de l'image d'illustration*</label>
                <input type="text" class="form-control" id="imageDescription"name="imageDescription" required>
            </div>
            <div class="form-group">
                <label for="author">Auteur*</label>
                <select class="form-control" id="author" name="author">
                    {% for author in authorList %}
                        <option value="{{ author.id }}">{{ author.name }}</option>
                    {% endfor %}
                </select>
            </div>
            <div class="form-group">
                <label for="chapterDescription">Description du chapitre*</label>
                <input type="text" class="form-control" id="chapterDescription" name="chapterDescription" required>
            </div>
            <div class="form-group">
                <label for="chapterText">Corps du texte*</label>
                <textarea class="form-control" id="chapterText" name="chapterText" rows="15" ></textarea>
            </div>
            <button type="submit" class="btn btn-primary float-right">Ajouter l'article</button>
        </form>
    </div>
{% endblock %}